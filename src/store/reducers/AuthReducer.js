import React from 'react';

const initState = {
    loading: false,
    registerErrors: [],
    loadingErrors: [],
}

const AuthReducer = (state = initState, action) => {
    return state;
};

export default AuthReducer;